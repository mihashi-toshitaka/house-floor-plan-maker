# 住宅平面図Webエディタ 作業進捗ログ

## 2025-01-01
### 実施内容
- 作業計画書（work-plan.md）を作成し、MVP要件・開発フェーズ・UX構成・テスト計画を整理。

### 進行状況
- フェーズ1（プロトタイプ）開始前。

### 次に進めるための確認事項（要決定）
1. フロントエンドの採用技術はどれにしますか？
   - 候補: React / Vue / Svelte
2. 描画エンジンの方針はどれにしますか？
   - 候補: SVGベース / Canvasベース
   - ライブラリ候補: Konva / Fabric.js / Paper.js
3. 初期MVPで必ず入れたいパーツはどれですか？
   - 例: 壁、ドア、窓、部屋ラベル、家具（ベッド/テーブル）など
4. 保存形式の仕様はどうしますか？
   - JSONのみ / JSON+画像書き出し
5. UI言語は日本語固定でよいですか？（将来i18nの必要有無）

### 次の作業予定（決定後に着手）
- 仕様決定に基づく技術スタック確定
- プロトタイプのリポジトリ構成・初期セットアップ

## 2025-01-02
### 決定事項
- フロントエンド: React
- 描画エンジン: Canvasベース
- 初期MVP必須パーツ: 壁・ドア・窓・部屋ラベル・備え付き設備（キッチン/浴槽/洗面台/トイレ）
- 保存形式: JSONのみ
- UI言語: 日本語固定

### 実施内容
- React + Vite の初期構成を追加（CanvasベースのUI骨格）。
- パーツパレット／キャンバス領域／プロパティパネルの基本レイアウトを作成。

### 進行状況
- フェーズ1（プロトタイプ）開始、UI骨格まで完了。

### 次に進めるための確認事項（要決定）
1. まず最初に実装したい操作はどれですか？
   - 例: ドラッグ&ドロップ配置 / 選択 / 移動 / リサイズ / 回転
2. 初期キャンバスのサイズや単位（mm/px）の基準はありますか？
3. 主要なスナップ単位は何mmにしますか？（例: 50mm）

### 次の作業予定（決定後に着手）
- Canvas上でのドラッグ&ドロップ配置の基礎実装
- 図形データのJSONモデル雛形作成

## 2026-01-29
### 実施内容
- `npm install` で依存解決エラー（eslint peer dependency）を確認。
- `npm install --legacy-peer-deps` で依存関係を導入。
- `npm run build` でビルド成功を確認。

### 実施内容（追加）
- 依存関係の最新安定版を調査し、React 19 / Vite 7 / ESLint 9 系に更新。
- eslint プラグイン群の peer dependency を確認し、ESLint 9 と互換の版に揃えた。
- `npm install` と `npm run build` を実行し、最新依存の状態でビルド成功を確認。
- `package-lock.json` をコミット対象外にし、`.gitignore` に追加した。

## 2026-01-30
### 実施内容
- eslint を `^8.57.0` に調整して eslint-plugin-react-hooks の peer dependency と整合させた。
- `npm install` を再実行し、`--legacy-peer-deps` なしで依存解決できることを確認。
- `package-lock.json` はコミット対象外のため削除。
- 開発用に Context7 MCP の設定を `.cursor/mcp.json` に追加。

### 進行状況
- 依存解決の回避策を利用した上でビルド可能。
- 依存解決の回避策なしでインストール可能。

### 次に進めるための確認事項（要決定）
- eslint / eslint-plugin-react-hooks の依存関係を正規化するか確認。

## 2026-01-31
### 実施内容
- AGENTS.md に「技術確認の際は context7 の利用も行う」指針を追加。
- `.cursor/mcp.json` から Context7 の env 設定を削除。
